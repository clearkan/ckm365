# Temporary / scratch (includes softeria-ref clone and requirements drafts)
tmp/

# Secrets — env vars or OS keychain only, never in repo
.env
.env.*
!.env.example
*.pem
*.pfx
*.key

# Python
__pycache__/
*.py[cod]
.venv/
*.egg-info/
.pytest_cache/
.ruff_cache/
.mypy_cache/
dist/
build/

# Token caches (file fallback lives outside repo, belt-and-braces)
.token_cache*
*.token.json

# Editors / OS
.idea/
.vscode/
*.swp
.DS_Store

# Subagent worktrees (never tracked)
.claude/worktrees/
