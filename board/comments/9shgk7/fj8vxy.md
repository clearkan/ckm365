---
type: comment
at: "2026-09-16T00:10:00Z"
by: "claude"
event: "moved"
---

Shipped in 2.7.0 as create_persona_reply, the PROPOSED TOOL at the bottom of this ticket, built as specified. It reads the original from wherever it lives, builds the MIME with the threading chain and the persona From, base64 CTE, imports to the persona's Drafts and returns the Draft. Write tier - nothing leaves the tenant, a human still reviews and sends. Graph.request() gained a content= raw-body pass-through to carry the import, which is what the implementation note predicted; the offline suite reaches it through MockTransport, so the tool is testable where the hand-rolled httpx recipe was not. ALSO FIXED HERE, the third bug in this cluster: export_message tagged Send-As mail inbound. Direction now comes from WHICH FOLDER the message is in, with the sender comparison kept only as the fallback outside Sent Items, exactly as this ticket proposed. Message gains parent_folder_id. VERIFIED LIVE ON BOTH TENANTS: from == the persona, In-Reply-To preserved against the counterparty's real Message-ID, References present, subject RE:-prefixed, quoted history inline, and the body text intact - that last one is the QP check, using a phrase containing "set the brief", the exact string that lost a character in August. verify_message reports boundary=fence on the imported draft, and revise_draft then REPLACES in place through it, so the CKM-48 fence survives the MIME route too. Draft discarded with a 404 confirming no residue. tests/test_live.py 14 passed on both tenants. Offline 169 passed / 15 skipped, covering the import target folder, the persona From, the threading headers, base64-not-QP on every part, reply_all dropping the persona from its own recipients, the fence, write-tier gating, and both directions of the export fix. NOT DONE, deliberately, and worth its own ticket if it is still wanted: fix shape 1, setting from= on an ordinary create_draft when mailbox= is an explicit shared mailbox. create_persona_reply covers the case that actually hurt - a reply that must thread - and it does so without guessing at intent from the mailbox argument. A create_draft that silently rewrites its own From is a bigger behavioural change than it looks, and it would fail at send time for anyone without Send As. docs/graph-direct.md Recipe 4 now points at the tool and is kept only for the mechanism.
