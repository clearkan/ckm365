id: CKM-5
title: Auth — app-only client credential + Exchange RBAC for Applications (interactive)
column: done
status: done
created_at: '2026-07-29T21:47:41Z'
updated_at: '2026-07-30T23:17:45Z'
assignees: []
description: |-
  INTERACTIVE with seanwy — walk through cmdlets, no unattended tenant changes.
  MULTI-TENANT: RBAC scoping is per tenant; repeat for each profile that
  needs app-only mode (mailbox lists differ per tenant).

  - Application permissions Mail.ReadWrite + Calendars.ReadWrite, scoped via
    RBAC for Applications in Exchange Online (NOT legacy Application Access
    Policy): management scope covering the operator's, ops@, tst.* mailboxes;
    assign via New-ManagementRoleAssignment
  - Certificate credential preferred over client secret

  ClearKan asks folded in (tmp/clearkan-integration-requirements.md item 5;
  their headless intake daemon DEPENDS on this issue — device-code auth is
  impossible in their containers):
  - 5.1 Live-verify the app-only code path end to end on one tenant
    (doctor, live-smoke, CKM365_LIVE_ACCOUNT=<profile>-app pytest
    tests/test_live.py).
  - 5.2 Specifically confirm the delta/watch tools (list_new_messages /
    wait_for_message) work app-only — delta bootstrap + token round-trip
    with application permissions; report any behavioral difference.
  - 5.3 Exchange RBAC scoping runbook (docs/app-only-setup.md,
    placeholders only, cert-preferred) — their enterprise-review artifact.

  SAFETY ORDER (non-negotiable, from our requirements doc + review): the
  Exchange RBAC management scope MUST exist before any app-only credential
  verification — an unscoped application permission can read EVERY mailbox
  in the tenant, even "briefly for testing". Scope first (or same
  interactive sitting), then verify. Also verify the NEGATIVE: app-only
  profile must get 403/404 on a mailbox outside the management scope.
dependencies:
- CKM-4
parent_id: null
priority: high
issue_type: task
tags: [auth, interactive]
requested_by: seanwy
due_date: null
history:
- at: '2026-07-29T21:47:41Z'
  by: claude
  details: Issue created in column backlog
  event: created
- at: '2026-07-29T21:53:30Z'
  by: claude
  details: Noted per-tenant RBAC scoping (multi-tenant requirement)
  event: updated
- at: '2026-07-30T14:02:08Z'
  by: claude
  details: Folded in ClearKan asks 5.1-5.3 (app-only live verification incl.
    delta/watch tools, then RBAC runbook); noted ClearKan's intake daemon
    depends on this; recorded RBAC-scope-before-credential-verification
    safety order and the out-of-scope negative test
  event: updated
- at: '2026-07-30T14:13:08Z'
  by: claude
  details: 'Moved to todo — all non-interactive prep is DONE and the issue is
    ready for the interactive sitting with seanwy (pick ONE tenant, per
    ckm365 doctor). Prepared: scripts/add-app-permissions.sh
    (application role-type permissions, preserves delegated scopes, verifies
    against actual appRoleAssignments, --dry-run mode; bash -n + merge logic
    tested offline) and docs/app-only-setup.md (RBAC-FIRST ordering,
    Test-ServicePrincipalAuthorization pre-checks, cert-preferred credential,
    <name>-app local profile recipe, verification incl. the out-of-scope
    negative test). OPEN QUESTION for the sitting: Microsoft documents
    Exchange access as the UNION of Entra app-role grants and RBAC
    assignments, so RBAC-only (no tenant-wide consent) is proposed as the
    primary path and the negative test decides whether add-app-permissions.sh
    is used at all. Remaining work is 100% interactive.'
  event: moved
- at: '2026-07-30T14:52:38Z'
  by: claude
  details: 'Moved to doing — interactive sitting started with seanwy on the
    owner''s chosen tenant. Following docs/app-only-setup.md, RBAC-only path
    first.'
  event: moved
- at: '2026-07-30T21:40:37Z'
  by: claude
  details: 'Approach pivot per seanwy: fully scripted (Jenkins-ready), not
    hand-run cmdlets. Built: scripts/create-exo-automation-app.sh (one-time
    bootstrap of a dedicated cert-auth EXO automation app —
    Exchange.ManageAsApp + Entra directory role, grant-verified, prints
    CKM365_EXO_* env), scripts/exo-common.ps1 (Connect-Ckm365Exo: app-only
    from env, interactive fallback), scripts/setup-app-rbac.ps1 /
    teardown-app-rbac.ps1 (idempotent, dry-run default, positive+NEGATIVE
    Test-ServicePrincipalAuthorization probes, ckm365-* name guard);
    create/remove-test-mailbox.ps1 now auto-connect via the helper.
    docs/app-only-setup.md rewritten: section 0 (automation app, two-app
    separation, least-privilege recipient-admin option), scripted RBAC
    section, CI/Jenkins pipeline recipe. NOTE: no pwsh on the dev box —
    ps1 scripts are balance-checked only; first execution happens where
    pwsh + ExchangeOnlineManagement exist. Remaining interactive: run the
    bootstrap (az already authed to the chosen tenant).'
  event: updated
- at: '2026-07-30T23:17:45Z'
  by: claude
  details: 'DONE — live-verified end to end on the chosen tenant, fully
    scripted run.
    seanwy ran create-exo-automation-app.sh --yes (automation app, cert PFX,
    Exchange.ManageAsApp consent grant-verified, exchange-admin directory
    role verified); everything after was unattended: app-only EXO connect OK
    first try; create-test-mailbox.ps1 provisioned tst.apponly;
    setup-app-rbac.ps1 -Apply created SP registration + ckm365-app-scope +
    both Application role assignments, Test-ServicePrincipalAuthorization
    InScope True (scoped mailbox) / False (operator mailbox); Graph-app cert
    generated + uploaded; an app-only profile added (allow_send=false).
    VERIFIED (ClearKan asks 5.1/5.2): doctor OK; live-smoke app-only read OK
    and deny probe 403 ErrorAccessDenied on out-of-scope mailbox (the
    NEGATIVE test, at the Graph layer); full live suite 5/5 app-only;
    delta/watch app-only — bootstrap token, delegated profile sent a real
    message into the scoped mailbox, app-only wait_for_message caught it via
    token round-trip (matched 1, no behavioral difference vs delegated).
    KEY RESULT: RBAC-only is sufficient — ZERO Graph application
    permissions consented; add-app-permissions.sh not needed (kept in
    reserve). Runbook + usage-modes updated to verified status. Per-tenant
    repetition for the second tenant remains open (new issue if/when
    needed).'
  event: moved
version: 1
