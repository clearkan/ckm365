id: CKM-25
title: Teams option-(c) slice — Graph team/channel discovery + app-installation state
column: done
status: done
created_at: '2026-07-30T14:52:38Z'
updated_at: '2026-07-31T22:10:28Z'
assignees:
- claude
description: |-
  Implementation of the CKM-24 decision (option (c)): the only Teams
  functionality that moves into ckm365 is the genuinely Graph-shaped
  subset. Bot Framework messaging/webhooks stay downstream, permanently
  under this option.

  Scope (read-only, new `teams` preset):
  - Team discovery (`/teams` or `/me/joinedTeams` per auth mode) and
    channel discovery (`/teams/{id}/channels`) — replaces the downstream
    consumer hand-configuring team/channel/conversation IDs via env vars.
  - Teams app installation state (same caveat).

  HARD REQUIREMENT carried from the downstream survey: this is a separate
  reviewed consent slice with least-privilege READ scopes (candidates:
  Team.ReadBasic.All, Channel.ReadBasic.All, TeamsAppInstallation
  read scopes — verify exact names at implementation). It must be kept
  OUT of the Mail.*/mailbox authorization: a mail --write flag must never
  imply Teams reach, and the consent script must be a separate deliberate
  opt-in like add-send-scopes.sh. Consent = tenant-touching = interactive
  with seanwy.

  Not urgent (downstream said no deadline); pure additive module over
  graph.py, no structural change expected. Line budget applies.
dependencies:
- CKM-24
parent_id: null
priority: low
issue_type: feature
tags: [teams, graph, interactive]
requested_by: seanwy
due_date: null
history:
- at: '2026-07-30T14:52:38Z'
  by: claude
  details: Issue created in column backlog — the (c)-slice from the CKM-24
    decision; consent design mirrors the send-tier opt-in pattern
  event: created
- at: '2026-07-31T14:43:39Z'
  by: claude
  details: 'Code COMPLETE and released as 2.1.0; live verification is the
    only thing outstanding and it is gated on an interactive consent step.
    Built: tools/teams.py (list_teams / list_channels / list_installed_apps
    — read-only, org-scoped so no mailbox param, 22 code lines) +
    Team/Channel/InstalledApp models; teams preset with empty WRITE/SEND
    tiers; scripts/add-teams-scopes.sh (delegated Team.ReadBasic.All,
    Channel.ReadBasic.All, TeamsAppInstallation.ReadForTeam; dry-run
    default; MERGES into existing requiredResourceAccess so mail scopes
    survive; grant-verified consent loop); live-smoke --teams.
    DESIGN CALL: "all" preset now means mail+calendar and EXCLUDES teams —
    a preset with its own consent tier must be named explicitly, so no
    existing session gains tools that would 403 or pay their schema cost.
    VERIFIED: offline 67 passed (auth-mode endpoint switch delegated
    /me/joinedTeams vs app-only /teams, projection, id encoding as a single
    segment, no mailbox param, preset counts); consent script dry-run
    resolved all three scope ids against real Graph; live-smoke --teams on
    the operator tenant correctly reports the pre-consent 403 as a SKIP with mail path
    still green. PENDING (seanwy, tenant-touching): run
    ./scripts/add-teams-scopes.sh --yes on a tenant, re-login, then
    live-smoke --teams to verify the post-consent reads — especially that
    $select is honoured on /me/joinedTeams, which offline mocks cannot
    prove. Issue stays in doing until that runs.'
  event: updated
version: 1
